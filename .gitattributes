# The bundled output in dist/ is generated by ncc and normalized to LF by
# scripts/normalize-dist.mjs. Pin it to LF in the repo so the CI dist-freshness
# check passes regardless of the contributor's OS.
dist/** text eol=lf
